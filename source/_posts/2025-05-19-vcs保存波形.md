---
title: vcs保存波形
date: 2025-05-19 16:55:41
categories:
- ip开发环境搭建
- EDA工具使用
tags:
- vcs
- verdi
---



# 保存fsdb波形

1、vcs添加编译选项：

```bash
-debug_access+all+dmptf -debug_pp +memcbk -debug_region=cell+lib -Mupdate
```

Note：
-debug_access+all+dmptf是新版本的dump机制
-debug_all 是旧版本的dump机制，且需要结合-P novas.tab以及pli.tab使用
<font color=blue>这两种方式不能同时使用，否则会dump fsdb 失败</font>

2、testbench中添加task语句

> ```verilog
> initial begin
>     $fsdbAutoSwitchDumpfile(500,"kite_execution_tb.fsdb",150);
>     $fsdbDumpvars(0, kite_execution_tb,"+mda");
> end
> ```

# verdi使用vcs生成的中间文件

1、vcs添加编译选项

```bash
-kdb
# 仿真目录下会生成 ./simv.daidir/kdb.elab++/
```



2、verdi加载

```bash
verdi -elab ./simv.daidir/kdb.elab++/ -ssf kite_execution_tb_000.fsdb &
```

