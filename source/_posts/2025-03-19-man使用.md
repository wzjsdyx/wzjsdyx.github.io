---
title: man使用
date: 2025-03-19 21:11:51
categories:
- 一生一芯计划
- 预学习阶段
tags:
- RISC-V CPU设计
---



```bash
$ man man 
```



> **DESCRIPTION**
>
> man  is  the system's manual pager.  
>
> Each page argument given to man is normally the name of a `program, utility or function`.  The manual page associated with each of these arguments is then found and displayed.  
>
> 
>
> **手册章节**
>
> - A `section`, if provided, will direct man to look only in that section of the manual.  
> - The default action is to search in all of  the available sections following a pre-defined order (see DEFAULTS), and to show only the first page found, even if page exists in several sections.
>
> The table below shows the section numbers of the manual followed by the types of pages they contain.
>
>    1   Executable programs or shell commands
>    2   System calls (functions provided by the kernel)
>    3   Library calls (functions within program libraries)
>    4   Special files (usually found in /dev)
>    5   File formats and conventions, e.g. /etc/passwd
>    6   Games
>    7   Miscellaneous (including macro packages and conventions), e.g. man(7), groff(7), man-pages(7)
>    8   System administration commands (usually only for root)
>    9   Kernel routines [Non standard]
>
> 
>
> **page章节**
>
> A `manual page` consists of several sections.
>
> Conventional  section  names  include  NAME, SYNOPSIS, CONFIGURATION, DESCRIPTION, OPTIONS, EXIT STATUS, RETURN VALUE, ERRORS, ENVIRONMENT, FILES, VERSIONS, CONFORMING TO,   NOTES, BUGS, EXAMPLE, AUTHORS, and SEE ALSO.

## 概念1：program, utility or function

> 📌 1.**Program（程序）**
>
> - 指的是**可执行的应用程序或命令**，你在终端输入并运行的东西。
>
> - 比如：
>
>   ```bash
>   man ls
>   ```
>
>   这里 `ls` 是一个程序，用于列出目录内容。
>
> ➡ 通俗理解：**用户直接运行的命令或工具。**
>
> ------
>
> 📌 2. **Utility（工具/实用工具）**
>
> - 实际上和 `program` 很相近，但更强调这是一个**系统提供的小工具或实用命令**，通常是做某种专门任务的小程序。
> - 比如：
>   - `grep` 是一个文本查找工具（utility）。
>   - `cp` 是一个文件复制工具。
>   - `df` 是磁盘空间查看工具。
>
> ➡ 通俗理解：**提供特定系统功能的小工具命令。**
>
> ------
>
> 📌 3. **Function（函数）**
>
> - 指的是**程序中使用的函数**，通常来自 **标准 C 库** 或其它系统库。
>
> - 这些不是你在终端直接运行的，而是程序开发者在代码中调用的。
>
> - 比如：
>
>   ```bash
>   man 3 printf
>   ```
>
>   `printf` 是一个 库函数，用于格式化输出，是写代码时用的。
>
> ➡ 通俗理解：**程序代码中调用的功能模块，而不是命令。**

## 概念2：section

> 在 Linux 系统中，手册（man pages）被**按照内容类型划分为不同的“章节”**，我们称为 **sections**。
> 每一节都对应一类信息，例如命令、系统调用、函数、配置文件等。

| 节号 | 内容                                   |
| ---- | -------------------------------------- |
| 1    | 命令（可执行程序）                     |
| 2    | 系统调用（内核函数，如 `open()`）      |
| 3    | C 语言库函数（如 `printf()`）          |
| 4    | 特殊文件（如 `/dev` 下的设备）         |
| 5    | 配置文件格式与规范（如 `/etc/passwd`） |
| 6    | 游戏                                   |
| 7    | 杂项（如 man 手册规范）                |
| 8    | 管理命令（系统管理员用的）             |

<font color=blue>如果手动指定了某一节，那么只在该节中查找需要的内容</font>

<font color=blue>如果不指定节号，那么按照顺序从`1-8`中查找，默认只显示第一个查找到的内容；</font>

``` bash
$ man -k printf # 查找某个命令属于哪一个section
asprintf (3)         - print to allocated string
dprintf (3)          - formatted output conversion
fprintf (3)          - formatted output conversion
fwprintf (3)         - formatted wide-character output conversion
printf (1)           - format and print data
printf (3)           - formatted output conversion
set_matchpathcon_printf (3) - set flags controlling the operation of matchpathcon or matchpathcon_index and configure the behaviour of validity checking and error displaying
snprintf (3)         - formatted output conversion
sprintf (3)          - formatted output conversion
swprintf (3)         - formatted wide-character output conversion
vasprintf (3)        - print to allocated string
vdprintf (3)         - formatted output conversion
vfprintf (3)         - formatted output conversion
vfwprintf (3)        - formatted wide-character output conversion
vprintf (3)          - formatted output conversion
vsnprintf (3)        - formatted output conversion
vsprintf (3)         - formatted output conversion
vswprintf (3)        - formatted wide-character output conversion
vwprintf (3)         - formatted wide-character output conversion
wprintf (3)          - formatted wide-character output conversion
XtAsprintf (3)       - memory management functions

$ man 1 printf #在命令中查找
$ man 3 printf #在库函数中查找
```

