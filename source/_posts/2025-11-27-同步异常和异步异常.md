---
title: 同步异常和异步异常
date: 2025-11-27 14:36:13
categories:
- ARM
- ARMv8-A架构
tags:
---

## 🔍 同步异常 vs 异步异常 —— 为什么影响不同？

### 🟢 同步异常（如：除0、访问非法地址、指令未定义）

- 异常**一定是由当前指令触发**
- 触发点 **精确对齐某条指令**
- 因此 CPU 必须把状态 **整理成“这条指令之前全部完成，这条指令之后全部未执行”**

👉 所以在同步异常发生前：

- CPU 会强制**清空写缓冲**
- **完成必要的 cache 写回**
- **禁止乱序对外可见的影响**
- 最终达到 **精确异常（precise exception）语义**

所以虽然内部有乱序 / 缓冲 / cache，但 CPU 会为了精确异常而把状态整理干净，然后**异常可恢复**。

------

### 🔴 异步异常（如：外部中断、DMA报警、定时器）

- 异常**不是由当前指令触发**
- 触发点**和正在执行的指令无关系**
- CPU **不能提前预测在哪里需要保存状态**

👉 异步异常发生时可能出现：

| 状态                   | 同步异常   | 异步异常   |
| ---------------------- | ---------- | ---------- |
| 重排已完成？           | 会等到完成 | 可能未完成 |
| 写缓冲是否已清空？     | 会强制清空 | 可能还积累 |
| cache 脏数据是否写回？ | 必须写回   | 可能未写回 |
| 程序可恢复性           | 高         | 低         |

因此，异步异常发生时，系统可能看到：

- 有的内存写已完成，有的未完成
- 顺序不符合程序语义
- 甚至可能回滚掉 cache 写入的值（write-back 冲刷）

继续执行会产生**不可预测错误**，因此异步异常通常**不可恢复**，处理方式往往是：

- 调度到中断服务程序，
- 不直接返回到引发点（除非架构定义了精确中断机制，例如 ARM GIC 支持 IRQ/FIQ 的异常返回）
