---
title: Ubuntu探索
date: 2025-03-11 22:46:00
categories:
- 一生一芯计划
- 预学习阶段
tags:
- RISC-V CPU设计
---

> 📌一个相对比较大的概念，如果某部分内部比较多或者重要，就会作为一个单独的页面编辑；



# 基本理解



## shell命令和程序

<font color=blue>Shell 命令 是由 shell 直接实现的命令，主要用于控制 shell 环境。</font>

<font color=blue>程序是独立的可执行文件，功能更复杂，用途更广泛。</font>

<font color=blue>使用 `type` 命令可以判断一个命令是 shell 命令还是程序。</font>



### Shell 命令

- **定义**：
  <font color=blue>shell 命令是直接由 shell（如 `bash`、`zsh`）解释和执行的命令。</font>
- **特点**：
  - <font color=blue>通常是 shell 内置的命令</font>
  - 执行速度快，因为<font color=blue>不需要启动新的进程</font>
  - 主要用于控制 shell 的行为或操作 shell 环境。
- **示例**：

| 命令      | 作用                                  |
| :-------- | :------------------------------------ |
| `cd`      | 改变当前工作目录                      |
| `echo`    | 输出文本到终端                        |
| `export`  | 设置环境变量                          |
| `source`  | 执行脚本文件并在当前 shell 环境中生效 |
| `alias`   | 创建命令别名                          |
| `unalias` | 删除命令别名                          |
| `set`     | 设置 shell 选项                       |
| `unset`   | 取消设置变量或函数                    |
| `read`    | 从标准输入读取数据                    |
| `exit`    | 退出当前 shell                        |

### 程序

- **定义**：
  <font color=blue>程序是独立的可执行文件，通常存储在 `/bin`、`/usr/bin` 等目录中。</font>
- **特点**：
  - <font color=blue>由 shell 调用并运行在新的进程中</font>
  - 功能更复杂，通常用于完成特定任务。
  - 可以是系统自带的工具，也可以是用户安装的软件。
- **示例**：

以下是一些常见的程序及其作用：

| 命令    | 作用                   |
| :------ | :--------------------- |
| `ls`    | 列出目录内容           |
| `find`  | 查找文件               |
| `grep`  | 搜索文本               |
| `vim`   | 文本编辑器             |
| `cat`   | 显示文件内容           |
| `mkdir` | 创建目录               |
| `rm`    | 删除文件或目录         |
| `cp`    | 复制文件或目录         |
| `mv`    | 移动或重命名文件或目录 |
| `ps`    | 显示进程状态           |



## 通配符与正则表达式

<font color=blue>一句话总结区别：通配符（wildcards）是文件名匹配模式，正则表达式（regex）是字符串匹配规则</font>

### 通配符（Wildcards）

- **特点**

  - 用在 **Shell（bash）或某些命令（ls、find）** 中。

  - 用来**匹配文件名**。

- **常见通配符**

| 通配符  | 含义                      | 示例                                         |
| ------- | ------------------------- | -------------------------------------------- |
| `*`     | 匹配任意字符（0个或多个） | `*.txt` 匹配 `a.txt`, `b.txt`                |
| `?`     | 匹配任意单个字符          | `?.txt` 匹配 `a.txt`, `b.txt`                |
| `[abc]` | 匹配括号内任意一个字符    | `file[12].txt` 匹配 `file1.txt`, `file2.txt` |

**常见通配符**

```bash
ls *.sh      # 匹配所有以 .sh 结尾的文件
find . -name "*.log"
```

### 正则表达式（Regex）

- **特点**
  - 是 **一种更强大的文本匹配规则**，常用于grep/sed/awk/perl/Python脚本等
- **常见正则符号**

| 正则符  | 含义                       | 示例                           |
| ------- | -------------------------- | ------------------------------ |
| `.`     | 任意一个字符               | `a.c` 匹配 `abc`、`a1c`        |
| `.*`    | 任意长度任意字符           | `a.*c` 匹配 `abc`、`axyzc`     |
| `^`     | 行首                       | `^hello` 匹配以 hello 开头的行 |
| `$`     | 行尾                       | `world$` 匹配以 world 结尾的行 |
| `[a-z]` | 匹配 a 到 z 之间的任意字符 | `[0-9]+` 匹配数字              |

```bash
grep "^error" log.txt       # 匹配以 error 开头的行
grep "file[0-9]\.txt" .     # 匹配 file0.txt ~ file9.txt
```

## shell命令的单引号和双引号

- 单引号

当你使用单引号包裹字符串时，Shell 会**严格地保留其中的字符原样**，这意味着**不对其中的任何特殊字符进行扩展或替换**。特别是其中的 `$`、`*`、`?` 等特殊字符也不会被展开。

- 双引号

当你使用双引号包裹字符串时，Shell 会对其中的一些特殊字符（如 `$`、反引号 等）进行扩展，但是对于 **`\*`**、**`?`** 和 **`[]`** 这些字符，Shell 会 **进行匹配**，而不会被“保留原样”。



## ls/find/grep匹配

ls和find是使用通配符进行匹配，但是ls和find运行时对通配符解释的时机不同；

grep是使用正则表达式进行匹配



> 假设有三个文件：file1.txt  file2.txt  file3.log
>
> 📌  `ls *.txt`
>
> 这个命令运行时，其实是 shell 做了这件事：
>
> ```bash
> ls file1.txt file2.txt
> ```
>
>  `ls` 程序根本看不到 `*.txt`，它接收到的是两个具体文件名，是 **shell 提前帮你匹配好了**。
>
> 
>
>  📌  `find . -name '*.txt'`
>
> 这个命令运行时，shell只是把 `*.txt` 这个字符串原样传给 `find`，find 自己再去查：
>
> ```bash
> 执行的是 find 程序，参数是字符串 '*.txt'
> ```
>
>  **find 内部才去判断**哪些文件名和 `'*.txt'` 匹配，它使用自己的匹配机制（不是正则，是 shell-style 匹配）。
>
> <font color=red>NOTE：`find . -name *.txt`</font>
>
> **不加引号**，shell 会先把 `*.txt` 展开成 `file1.txt file2.txt`
>
> ```bash
> 此时，执行的是find . -name file1.txt file2.txt  # 错了，语法不对
> 所以才说：在 find 中要加引号，这样 shell 就不会抢着处理通配符。
> ```
>
> 
>
> 📌` grep 'D*p'  file3.log`
>
> 这个命令运行时，shell只是把 `a*c` 这个字符串原样传给 `grep`，让grep自己再去做正则匹配；
>
> 通常加引号，防止正则符号被 shell 误解释





# 常见命令&使用场景

## 磁盘空间占用df

```shell
zhijun@Thinkbook:/root$ df -h
Filesystem      Size  Used Avail Use% Mounted on
tmpfs           790M  2.1M  788M   1% /run
/dev/sda3       491G   13G  454G   3% /
tmpfs           3.9G     0  3.9G   0% /dev/shm
tmpfs           5.0M  4.0K  5.0M   1% /run/lock
/dev/sda2       512M  6.1M  506M   2% /boot/efi
tmpfs           790M  104K  790M   1% /run/user/1000
/dev/sr0        157M  157M     0 100% /media/zhijun/CDROM
/dev/sr1        4.5G  4.5G     0 100% /media/zhijun/Ubuntu 22.04.5 LTS amd64

# df: disk free
# 显示文件系统的磁盘空间使用情况
```

## 关机poweroff

```shell
poweroff
```

## 切换用户su

```shell
su -
su $username
```



## sort 排序

```shell
$ sort
NAME
       sort - sort lines of text files
DESCRIPTION
       Write sorted concatenation of all FILE(s) to standard output
       -h, --human-numeric-sort
              compare human readable numbers (e.g., 2K 1G)
       -n, --numeric-sort
              compare according to string numerical value       
       -r, --reverse
              reverse the result of comparisons # 从大到小排序，默认从小到大排序              
```



## 查看磁盘使用情况

```shell
$ du -sch * | sort 
Summarize disk usage of the set of FILEs, recursively for directories. # 默认会递归文件夹
       -d, --max-depth=N
              print the total for a directory (or file, with --all) only if it is N or fewer levels below the command line argument;  --max-depth=0 is the same as --summarize # 控制显示的层数；如果是0层，效果和-s选项相同
       -s, --summarize
              display only a total for each argument #针对du的参数，只显示当前层的大小，不递归显示
       -c, --total
              produce a grand total #在最后产生一个总计       
       -h, --human-readable
              print sizes in human readable format (e.g., 1K 234M 2G)              
```

## 管道

连接起两个命令的关键就是管道符号`|`. 这一符号的左右都是Shell命令, `A | B`的含义是创建两个进程`A`和`B`, 并将`A`进程的标准输出连接到`B`进程的标准输入；

```bash
$ ll -a | grep "hello"
```



## 重定向

`>`标准输出重定向；

`<`标准输出重定向；

```bash
$ ll -a > filelist.f
# > 是标准输出重定向符号, 可以将前一命令的输出重定向到文件filelist.f, 此时terminal上就无法显示内容；
# 如果既需要重定向到文件中，又想要显示在terminal上，需要使用tee命令
$ ll -a | tee filelist.f
	
# /dev/null是一个特殊的文件，任何输出都此文件中的数据都会被丢弃；
```

​	



# 更新软件源并安装软件

linux是通过`包管理`来安装或者卸载软件，Ubuntu的包管理软件是`apt`

需要注意，<font color=blue>系统版本和软件源相匹配</font>

## Checking network state

```shell
ping www.baidu.com -c 4
```

## Setting APT source file

修改APT源文件需要root权限；

```shell
# step1：将用户添加到sudo group中
# sudo组是系统中一个特殊的用户组，当一个用户被添加到sudo组后，这个用户就获得了使用sudo命令的权限
# sudo命令会临时提升权限到 root 用户
su -
adduser username sudo  # Change `username` to your user name
exit

# step2：验证用户是否有使用sudo命令的权限
# whoami命令用于显示当前用户的用户名
# sudo whoami，由于sudo会临时提升权限到 root 用户，然后以root用户执行whoami，因此会输出root
whoami
sudo whoami

# step3：修改APT source file
# 也可以直接修改/etc/apt/sources.list文件
sudo sed -i "s/archive.ubuntu.com/mirrors.tuna.tsinghua.edu.cn/g" /etc/apt/sources.list

# step4:确认替换是否成功
cat /etc/apt/sources.list

# step5: 更新软件源
sudo apt-get update

# step6: 安装软件
sudo apt-get install build-essential    # build-essential packages, include binary utilities, gcc, make, and so on
sudo apt-get install man                # on-line reference manual
sudo apt-get install gcc-doc            # on-line reference manual for gcc
sudo apt-get install gdb                # GNU debugger
sudo apt-get install git                # revision control system
sudo apt-get install libreadline-dev    # a library used later
sudo apt-get install libsdl2-dev        # a library used later
```

> - `sed`：
>   - `sed` 是一个流编辑器（stream editor），用于对文本进行处理和转换。
> - `-i`：
>   - 表示直接修改文件内容（in-place edit）。
> - `"s/archive.ubuntu.com/mirrors.tuna.tsinghua.edu.cn/g"`：
>   - `s` 表示替换操作（substitute）。
>   - `archive.ubuntu.com` 是要查找的字符串。
>   - `mirrors.tuna.tsinghua.edu.cn` 是要替换成的字符串。
>   - `g` 表示全局替换（global），即替换文件中所有匹配的字符串。
> - `/etc/apt/sources.list`：
>   - 这是 Ubuntu 系统中存储软件源地址的文件。
>
> ---
>
> `cat` 是 Linux 和 Unix 系统中的一个常用命令，全称是 **concatenate**（连接）：
>
> 它的主要功能是**显示文件内容**，
>
> 也可以用于**创建文件**、**合并文件**等操作



# GDB工具

[GDB debugging tutorial for beginners](https://linuxconfig.org/gdb-debugging-tutorial-for-beginners)



# vim工具

单独编辑

```bash
" All system-wide defaults are set in $VIMRUNTIME/debian.vim and sourced by
" the call to :runtime you can find below.  If you wish to change any of those
" settings, you should do it in this file (/etc/vim/vimrc), since debian.vim
" will be overwritten everytime an upgrade of the vim packages is performed.
" It is recommended to make changes after sourcing debian.vim since it alters
" the value of the 'compatible' option.

runtime! debian.vim

" Vim will load $VIMRUNTIME/defaults.vim if the user does not have a vimrc.
" This happens after /etc/vim/vimrc(.local) are loaded, so it will override
" any settings in these files.
" If you don't want that to happen, uncomment the below line to prevent
" defaults.vim from being loaded.
" let g:skip_defaults_vim = 1

" Uncomment the next line to make Vim more Vi-compatible
" NOTE: debian.vim sets 'nocompatible'.  Setting 'compatible' changes numerous
" options, so any other options should be set AFTER setting 'compatible'.
"set compatible

" Vim5 and later versions support syntax highlighting. Uncommenting the next
" line enables syntax highlighting by default.
if has("syntax")
  syntax on
endif

" If using a dark background within the editing area and syntax highlighting
" turn on this option as well
set background=dark

" Uncomment the following to have Vim jump to the last position when
" reopening a file
"au BufReadPost * if line("'\"") > 1 && line("'\"") <= line("$") | exe "normal! g'\"" | endif

" Uncomment the following to have Vim load indentation rules and plugins
" according to the detected filetype.
filetype plugin indent on

" The following are commented out as they cause vim to behave a lot
" differently from regular Vi. They are highly recommended though.
"set showcmd		" Show (partial) command in status line.
set showmatch		" Show matching brackets.
set ignorecase		" Do case insensitive matching
set smartcase		" Do smart case matching
set incsearch		" Incremental search
"set autowrite		" Automatically save before commands like :next and :make
set hidden		" Hide buffers when they are abandoned
set mouse=a		" Enable mouse usage (all modes)

" Source a global configuration file if available
if filereadable("/etc/vim/vimrc.local")
  source /etc/vim/vimrc.local
endif

setlocal noswapfile " 不要生成swap文件
set bufhidden=hide " 当buffer被丢弃的时候隐藏它
"colorscheme evening " 设定配色方案
set number " 显示行号
set cursorline cursorcolumn
highlight CursorLine cterm=NONE ctermbg=darkyellow ctermfg=white guibg=darkyellow guifg=white
highlight CursorColumn cterm=NONE ctermbg=darkyellow ctermfg=white guibg=darkyellow guifg=white
set ruler " 打开状态栏标尺
set shiftwidth=2 " 设定 << 和 >> 命令移动时的宽度为 2
set softtabstop=2 " 使得按退格键时可以一次删掉 2 个空格
set tabstop=2 " 设定 tab 长度为 2
set nobackup " 覆盖文件时不备份
set autochdir " 自动切换当前目录为当前文件所在的目录
set backupcopy=yes " 设置备份时的行为为覆盖
set hlsearch " 搜索时高亮显示被找到的文本
set noerrorbells " 关闭错误信息响铃
set novisualbell " 关闭使用可视响铃代替呼叫
set t_vb= " 置空错误铃声的终端代码
set matchtime=2 " 短暂跳转到匹配括号的时间
set magic " 设置魔术
set smartindent " 开启新行时使用智能自动缩进
set backspace=indent,eol,start " 不设定在插入状态无法用退格键和 Delete 键删除回车符
set cmdheight=1 " 设定命令行的行数为 1
set laststatus=2 " 显示状态栏 (默认值为 1, 无法显示状态栏)
set statusline=\ %<%F[%1*%M%*%n%R%H]%=\ %y\ %0(%{&fileformat}\ %{&encoding}\ Ln\ %l,\ Col\ %c/%L%) " 设置在状态行显示的信息
set foldenable " 开始折叠
set foldmethod=syntax " 设置语法折叠
set foldcolumn=0 " 设置折叠区域的宽度
setlocal foldlevel=1 " 设置折叠层数为 1
nnoremap <space> @=((foldclosed(line('.')) < 0) ? 'zc' : 'zo')<CR> " 用空格键来开关折叠

" 启用高亮显示行尾空格和 Tab
set list

" 设置 Tab、空格和行尾空格的显示符号
set listchars=tab:▸\ ,space:·,trail:·

" 设置 Tab 和空格的颜色
highlight SpecialKey ctermfg=8 guifg=#808080
```



# man工具

单独编辑

# Makefile管理

单独编辑

# 终端复用工具

##  tmux

`tmux- terminal multiplexor`

terminal上面只需要运行tmux一个进程即可；



### 基本介绍

`tmux创建出来的进程以及界面布局会保存到session`

用户关闭terminal窗口的时候，tmux进程也会退出，但是session中的进程会继续运行；下次进入tmux可以重新选择session，可以恢复原来的窗口布局；



### tmux使用

tmux的所有命令都是通过`ctrl+b`触发；



**不带参数的tmux调用**:

tmux会自动创建一个新的session，并且创建一个默认的shell。



**带有参数的tmux调用：**

```bash
tmux new -s new_session
```



**命令：**

左右分屏：`ctrl+b+%`

上下分屏：`ctrl+b+"`

切换焦点：`ctrl+b+方向键`



新窗口：如果当前屏幕的显示已经太多，使用`ctrl+b+c`开启新的windows;

切换窗口：`ctrl+b+窗口号`



detached session ：`ctrl+b+d，或者直接关闭terminal`，此时session中运行的进程并不会终止

session查看：`tmux ls`列出所有的session

attached session：`tmux a -t session号`，如果只有一个session，可以省略-t 选项

退出session：

- 逐个退出进程`exit`之后，session自动会退出；
- `tmux kill-session -t session号`







**tmux配置文件：**

```bash
touch ~/.tmux.conf
set -g mouse on — 开启鼠标支持
set -g prefix C-s — ctrl + b前缀改成ctrl + s
```





参考博客：

[tmux 使用和基础配置 从入门到加班，一个视频全搞定！](https://www.bilibili.com/video/BV1Pnche9EwY/?spm_id_from=333.337.search-card.all.click&vd_source=c76cab13994e90aef30da628f94d)



## Zellij

```bash
$ zellij --layout strider
```

<font color=red>遇到一个难搞的问题：复制粘贴非常不好用</font>

[Zellij – 颜值爆表，比tmux、screen更好用的多窗口终端](https://blog.csdn.net/hbuxiaofei/article/details/129641661)

[Zellij：一款Linux的现代终端多路复用器](https://www.a5idc.com/article/9344.html)

[Zellij 简介及使用技巧](https://sspai.com/prime/story/get-started-with-zellij)

[官方Zellij User Guide](https://zellij.dev/documentation/commands.html)

# Git工具

git log

git status

git diff



参考博客：

[Visualizing Git Concepts with D3](https://onlywei.github.io/explain-git-with-d3/#reset)

[官方git使用手册-中文版](https://git-scm.com/book/zh/v2)



---

# 计算机教育中缺失的一课

> 需要慢慢打磨工具的使用，能够提升工作效率

[The Missing Semester of Your CS Education 中文版](https://missing-semester-cn.github.io/)

# 其他

修改用户名：

[ubuntu修改用户名、主机名、主文件夹名、登录名、密码](https://download.csdn.net/blog/column/12800169/142705779)

