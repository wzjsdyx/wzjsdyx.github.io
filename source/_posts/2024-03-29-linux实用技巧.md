---
title: linux实用命令
date: 2024-03-29 09:36:02
categories:
- ip开发环境搭建
- linux&shell脚本
tags:
- TBD
---

<font color=red>TBD：待补充</font>

# 基本使用方法

## $?

>  `$?` 表示“刚刚执行的那一条命令的退出状态码（return code）”



# LSF



`LSF` 是 **IBM Spectrum LSF**，一种集群作业调度系统

```shell
bsub
bjobs
bqueues
bhosts
```

这些都是 LSF 的命令



## 队列 & 主机



```shell
bqueues -l RL7
```

> QUEUE: RL7
>   -- For HF_CENTOS7 jobs
>
> PARAMETERS/STATISTICS
> PRIO NICE STATUS          MAX JL/U JL/P JL/H NJOBS  PEND   RUN SSUSP USUSP  RSV PJOBS 
> 100   -1  Open:Active       -    -    -    -     9     0     9     0     0    0     0
>  TASKLIMIT               
>  12
>
> SCHEDULING PARAMETERS
>            r15s   r1m  r15m   ut      pg    io   ls    it    tmp    swp    mem
>  loadSched   -     -     -     -       -     -    -     -     -      -      -  
>  loadStop    -     -     -     -       -     -    -     -     -      -      -  
>
> <font color=blue>USERS: all</font>  
> <font color=blue>HOSTS:  CENTOS7_HOSTS</font>



```shell
bhosts
```

> HOST_NAME          STATUS       JL/U    MAX  NJOBS    RUN  SSUSP  USUSP    RSV 
> host001            ok              -    256     38     38      0      0      0
> host002            ok              -    256    140    140      0      0      0
> host003            ok              -    256     62     62      0      0      0
> host004            ok              -    256     22     22      0      0      0
> host005            ok              -     96     32     32      0      0      0
> host006            ok              -     96     13     13      0      0      0
> host007            ok              -     64      7      7      0      0      0
> host008            ok              -     64     18     18      0      0      0
> host009            ok              -     64     32     32      0      0      0
> host010            ok              -     64     40     40      0      0      0
> host011            ok              -     64     46     46      0      0      0
> host012            ok              -     64     11     11      0      0      0
> host013            ok              -     64     28     28      0      0      0
> host014            ok              -     64     21     21      0      0      0
> host015            ok              -     64     25     25      0      0      0
> host016            ok              -     64      6      6      0      0      0
> host017            ok              -     32      3      3      0      0      0
> host061            ok              -     12      1      1      0      0      0
> host062            ok              -     12      2      2      0      0      0
> host063            ok              -     12      0      0      0      0      0
> host064            ok              -     12      1      1      0      0      0
> host065            ok              -     12      0      0      0      0      0
> host066            ok              -     12      0      0      0      0      0
> host067            ok              -     12      1      1      0      0      0
> host068            ok              -     16      4      4      0      0      0
> jkhfhns01          closed          -      0      0      0      0      0      0
> jkhfhns02          closed          -      0      0      0      0      0      0



```shell
bmgroup
bugroup
```

> GROUP_NAME    HOSTS                     
> ManagementHo jkhfhns01 jkhfhns02 
> Public       host010 host011 host012 host001 host013 host002 host014 host003 host015 host004 host016 host005 host017 host006 host007 host008 host009 
> <font color=blue>CENTOS7_HOST host065 host066 host067 host068 host061 host062 host063 host064 </font>
> HF_BE_HOSTS  host002 
> HF_SUPER_HOS host003
>
> ===========================================================
>
> GROUP_NAME    USERS                     
> admin        atc0251 
> HF_BE_USERS  atc3020 atc0251 atc3012 atc3023 atc0295 atc3024 atc0296 atc3028 atc2105 atc3019 
> HF_BE2_USERS atc3020 atc0251 atc3012 atc3023 atc0295 atc3024 atc0296 atc3028 atc2105 atc3019 
> HF_SUPER_USE atc3020 atc0251 atc3012 atc3023 atc0296 atc3024 atc3028 atc2105 atc3019



## jobs提交执行

```shell
bsub
-q: 申请队列节点
-I：interactive，交互式
-s：通常表示给你一个 shell 环境

bsub -Is 是“本地终端做显示，远端节点真执行”

bsub -q RL7 -Is /bin/csh
向 LSF 集群提交一个作业，并指定提交到 `RL7` 这个队列，资源分配到以后，直接进入那台机器上的一个交互式 csh shell

bsub -q RL7 -Is cmd
cmd是是申请到的机器上的 shell，不是你本地登录节点原来的 shell
```

## 常见工作流

```shell
# 1. 先申请交互节点
bsub -q RL7 -Is /bin/csh

# 2. 进去后确认在哪台机器
hostname
uname -a # Unix name -a = all，尽可能把能显示的信息都显示出来，显示系统内核和主机的整体信息
cat /etc/os-release

# 3. 退出后再查队列详情
bqueues -l RL7
```





# 加解压



# diff文件

```shell
gvimdiff file1 file2
```

# 内存空间查询

## 详细的report

```bash
#!/bin/bash

# 获取当前用户
user=$USER

# 获取当前时间
echo "===== 当前用户内存使用报告 ====="
echo "用户: $user"
echo "时间: $(date)"

# 获取系统总物理内存（单位 GB）
total_phys_mem=$(free -g | awk '/^Mem:/ {print $2}')

# 当前用户物理/虚拟内存总和（GB）
read user_rss_gb user_vsz_gb <<< $(ps -u $user -o rss=,vsz= | \
  awk '{rss_sum+=$1; vsz_sum+=$2} END {printf "%.2f %.2f", rss_sum/1024/1024, vsz_sum/1024/1024}')

# 系统所有进程的虚拟内存总和（GB）
total_vsz_gb=$(ps -eo vsz= | awk '{vsz_sum+=$1} END {printf "%.2f", vsz_sum/1024/1024}')

echo
echo "===== 🧠 内存总览（单位 GB） ====="
echo "系统总物理内存:        $total_phys_mem GB"
echo "当前用户物理内存总和:  $user_rss_gb GB"
echo "当前用户虚拟内存总和:  $user_vsz_gb GB"
echo "系统全部虚拟内存总和:  $total_vsz_gb GB"
echo

# 打印表头
printf "%-8s %-20s %-12s %-12s\n" "PID" "COMMAND" "RSS(MB)" "VSZ(MB)"

# 打印当前用户每个进程的内存信息，按VSZ从大到小排序
ps -u $user -o pid,comm,rss,vsz --sort=-vsz | \
awk '
NR > 1 {
    rss_mb = $3 / 1024
    vsz_mb = $4 / 1024
    total_rss += rss_mb
    total_vsz += vsz_mb
    printf "%-8s %-20s %-12.2f %-12.2f\n", $1, $2, rss_mb, vsz_mb
}
END {
    print "------------------------------------------------------------"
    printf "%-8s %-20s %-12.2f %-12.2f\n", "TOTAL", "", total_rss, total_vsz
}'

```

> ---
>
> `ps`：process status显示当前运行中的进程
>
> `-u $USER`：只列出当前用户的进程
>
> `-o pid,comm,rss`：只输出这三列
>
> - `pid`：进程 ID
> - `comm`：命令名称（程序名）
> - `rss`：实际使用的物理内存（单位：KB)
>
> `--sort=-rss`：按内存使用量从大到小排序
>
> `awk`：文本处理工具





# 磁盘空间查询



## 用户磁盘配额

```shell
quota -s
# 查看配额并以易读的方式显示
```

`quota`命令用于显示用户的磁盘使用量和限制



## 当前目录磁盘占用空间

```shell
du -h --max-depth=1
```



> 这个命令的作用是：
>
> - `du`: 是 disk usage 的缩写，用于查看目录或文件所占用的磁盘空间大小。
> - `-h`: 以人类可读的格式显示文件大小，例如 GB、MB 等。
> - `--max-depth=1`: 限制显示的深度，即只显示当前目录下直接子目录的信息。
>
> 运行这个命令后，会列出当前目录下每个子目录的总大小，并以人类可读的方式显示（如 GB、MB 等）。



 **按大小排序显示**

```shell
du -sh * | sort -h
```

> `sort -h`：按人类可读的格式排序，从小到大排列



# 文件名查找

```shell
find ./ -name '*.pdf'
# 使用通配符
```







# 管道命令

```shell
grep "搜索内容" 文件名 | tee 搜索结果.txt
```

> `grep "搜索内容" 文件名`：这是用于搜索的命令。你可以替换 `"搜索内容"` 和 `文件名` 为你想要的内容和文件。
>
> `|`：管道符，将前一个命令的输出传递给下一个命令。
>
> `tee 搜索结果.txt`：`tee` 命令会将接收到的输出同时写入 `搜索结果.txt` 文件并显示在屏幕上。

# log打印

## 执行信息打印

### bash脚本

```shell
# 执行并记录
./tc.sh 2>&1 | tee tc.log

# 查看日志
cat tc.log

# 同时监控日志
tail -f tc.log
```

### tcsh脚本

```shell
# 执行脚本并将输出同时显示在屏幕和保存到文件
./tc.csh |& tee tc.log

# 追加模式
./tc.csh |& tee -a tc.log

# 带时间戳的日志
./tc.csh |& tee >(perl -ne 'print scalar(localtime), " ", $_') > tc.log
```



## shell中打印pwd

```shell
echo "$PWD"
```

