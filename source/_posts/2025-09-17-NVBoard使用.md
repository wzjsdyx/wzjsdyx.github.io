---
title: NVBoard使用
date: 2025-09-17 23:36:13
categories:
- 一生一芯计划
- E阶段
tags:
---



# Makefile解析

<font color=red>TBD</font>

# 注意点

如果使用NVBoard，需要不断调用`nvboard_update();`

电路有更新就需要调用`dut->eval();`



# Q&A:

## Makefile中的默认目标是什么？

有默认的名称吗？例如`all`或者`default`?

Makefile中的默认目标是`第一个目标`，没有默认的名字；<font color=blue>make执行的就是第一个目标；</font>

但是通常的Makefile脚本中，有些名字是约定俗成的：

- `all`：代表编译所有东西（最常见，约定俗成）

- `default`：有些项目用这个表示默认目标

- `install`：安装软件

- `clean`：清理文件

## Makefile中的变量赋值及调用

- `=`  普通赋值，每次展开时重新计算

> FOO = $(BAR)
> BAR = hello
>
> 👉 延迟展开：右边内容不会立刻求值，而是等到用到 `$(FOO)` 时再去展开。

- `:=` 立即展开赋值（只计算一次）

> FOO := $(BAR)
> BAR = hello
>
> 👉 立即展开：右边的内容在赋值那一刻就求值，以后再改 `BAR` 也不会影响 `FOO`。

- `?=` 条件赋值**：**只有当变量之前没有定义过时才会赋值

> FOO ?= hello
>
> 👉如果之前没定义过 `FOO`(即没有普通赋值定义或者命令行传入)，就赋值为 `hello`；如果用户或前面定义过，则保持原值。
>
> 很常见于跨平台项目，目的是给项目设定 **默认值**，但又允许用户在命令行覆盖

- `+=` 追加

> 变量追加

- 使用$(VAR)引用变量



## 特殊符号

- `$@` → 当前目标

- `$^` → 所有依赖

- `$<` → 第一个依赖



## Makefile的内置函数

| 表达式                 | 作用                      | 例子                   | 输出                     |
| ---------------------- | ------------------------- | ---------------------- | ------------------------ |
| `$(shell cmd)`         | 执行 shell 命令并返回结果 | `$(shell echo hi)`     | `hi`                     |
| `$(abspath path)`      | 转换成绝对路径            | `$(abspath ./src)`     | `/home/user/project/src` |
| `$(addprefix p, list)` | 给列表里每个元素加前缀    | `$(addprefix -I, a b)` | `-Ia -Ib`                |
