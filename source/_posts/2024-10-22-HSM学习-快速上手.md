---
title: HSM学习-快速上手
date: 2024-10-22 14:10:55
tags:
---



<font color=blue>With using this manual, you will know what it is and how to integrate it into a target SOC；</font>

**Audience**

This manual is written for hardware and software engineers with experience and knowledge of implementingSOC(whit security system) designs. Before using this manual, you should be familiar with the following topics:

- Cryptography Standards, such as GM/T

- Security System, such as Arm Platform Security Architecture



<font color=blue>注重基本概念理解</font>

# eHSM简介

**eHSM是设备的“根信任”**：在信息安全中，**根信任**是整个系统的信任基础。eHSM作为设备的根信任，负责确保关键的安全功能（如加密、密钥管理）与设备的其他部分硬件隔离开来，从而防止未经授权的访问或篡改。



**完整的硬件单元**：eHSM拥有独立的硬件模块，包括：

- 时钟（clock）：控制eHSM操作的时序。
- 复位（reset）：在系统启动或重启时重置硬件状态。
- 启动（boot）和总线系统（bus system）：负责管理系统的初始化和各硬件模块之间的数据传输。
- CPU和内存：内置的CPU和内存专门用于处理与安全相关的任务。
- 加密算法引擎系统：高性能的加密引擎，用于执行复杂的加密算法（如TRNG、HASH、SKE和PKE）。

每次eHSM被重置（例如设备启动时），它都会对自身的硬件系统进行自检，确保所有的硬件组件运行正常。这是为了确保eHSM自身的可靠性和安全性。



**安全服务**：eHSM为设备（如SoC，系统级芯片）提供多种信息安全相关的服务，包括：

- 安全存储：保护敏感信息（如密钥、证书等）的存储。
- 安全启动：确保设备从启动到运行的过程中不受未经授权的干扰。
- 安全生命周期管理：管理设备的不同安全状态（例如设备的制造、部署、使用和退役阶段）。
- 密钥管理：安全生成、存储和分发加密密钥。



**通信机制**：设备（SoC）通过**邮箱（mailbox）机制**与eHSM通信，使用其提供的加密服务（如TRNG随机数生成、哈希计算、对称和非对称加密）。此外，eHSM还提供了其他硬件接口，允许设备传输传感器数据、状态信号（例如警告和错误信号）等。

{% asset_img image-20241022142935631.png %}



## Feature List

The eHSM has the following features:

- Secure Storage

  - Sensitive Information Storage

  - Firmware image Storage

- Security Lifecycle

- Secure Boot

- Secure Debug
- Cryptographic Service
  - TRNG
  - HASH
  - SKE
  - PKE

- Self Check
- System CSR
- Error Management Unit
- Mailbox
- Peripherals
  - Random Clock
  - UART
  - Watchdog Timer
  - General Timer
  - UTC Timer
  - Monotonic Counter
  - CRC
- Bus Parity Check

## eHSM系统架构

- OTP and NVM in different storage

- OTP and NVM in same storage (emurate OTP w/ NVM)

- Only has OTP

{% asset_img image-20241022144937281.png %}

{% asset_img image-20241022144354988.png %}

{% asset_img image-20241022145551287.png %}

## Storage Requirement

{% asset_img image-20241022145807577.png %}



# secure storage

The eHSM provides a complete secure storage solution for storing `keys`, `sensitive information`, and `firmwareimages`.

## sensitive information

使用OTP去存储敏感信息，如下所示：

1.Life cycle

2.UID

3.HW/FW control options

4.eHSM error response control options

5.eHSM firmware version counter

6.SoC firmware version counter

7.OTP keys

The OTP could be a real OTP/eFuse or be emulated by the NVM which doesn’t support erase

### OTP Memory Map

{% asset_img image-20241022152529691.png%}



### OTP HW/FW Control Fields

{% asset_img image-20241022152841916.png%}

{% asset_img image-20241022152903816.png%}

{% asset_img image-20241022152949707.png%}



### Sensor Response

在信息安全领域，`传感器`通常指用于检测环境或系统中异常情况的设备。具体到eHSM或SoC（系统级芯片），传感器可以监测温度、电压、物理入侵或其他异常状态。当某个传感器检测到异常时，系统可以根据这些位的设置做出不同的响应，比如重置系统、清除密钥或停止加密服务。这些响应的设置确保系统在受到攻击或发生故障时能够采取保护措施。



### Key Manager

存放在OTP中的加密key，包含attributes和crc_code。

eHSM 硬件会自动读取key并check crc，通过之后会调用eHSM的CPU进行解密。

#### eHSM Keys

**层次结构**

{% asset_img image-20241022160817475.png%}

**key说明**

{% asset_img image-20241022162819839.png%}



#### OTP Key Fields

{% asset_img image-20241022160950542.png%}

**crc code**

<font color=blue>There is a special hardware CRC module(not the CRC peripheral) to compare the CRC results of thekeys when the eHSM is booting</font>



**key attributes**

{% asset_img image-20241022161221032.png%}

<font color=blue>Only valid OTP keys will be decrypted and saved to DRAM , otherwise, saved meaningless number.</font>



 <font color=red>#Q:key attributes和crc code是怎么生成?</font>

#### OTP Keys Installation

<font color=blue>It’s strongly recommended to generate and maintain all the OTP keys with a hardware secure module (eHSM).</font>

{% asset_img image-20241022161958915.png%}

## Firmware image

{% asset_img image-20241022162528049.png%}

# Security Lifecycle

eHSM lifecycle定义了设备的整个生命周期。lifecycle state定义了security properties。

lifecycle的state会影响：

- eHSM的内部资源的访问和更新；
- 硬件错误状态的处理；



# secure Boot



## Boot Requirement

- The OTP keys
- The current security lifecycle state
- Boot parameters
- Boot ROM code

## Hardware Boot Flow

 the hardware boot flow after eHSM’s reset released by SoC

{% asset_img image-20241022165347457.png%}

> - Hardware read the OTP fields  and save the value to the corresponding CSRfor firmware reading
>
> - When TRNG fails, inlcuding hardware self-test failure or waiting for RNG data ready timeout, the boot controller will reset the TRNG untill the reset times timeout, 
>
> - ”Key Error” includes OTP Key CRC Error or invalid Error(refer to4.2.5)



## Software boot flow

### boot flow

{% asset_img image-20241022165848566.png%}

<font color=red>#Q:AI11923A使用的是什么组合？</font>

Internal NVM + IRAM?

### install/update flow

{% asset_img image-20241022170102288.png%}

<font color=red>#Q:AI11923A使用的是什么组合？</font>

# secure Debug



eHSM secure debug architecture如下图所示：

{% asset_img image-20241022171845795.png%}



eHSM通过o_ehsm_status[17:16]控制eHSM和SoC的debug port;

{% asset_img image-20241022170942242.png%}

eHSM和SoC的debug port enable/disable依赖于lifecycle

{% asset_img image-20241022171123552.png%}





如果eHSM或者SoC debug port处于disable的时候（因为lifecycle的states），需要鉴权才能打开，此鉴权操作可以通过UART或者mailbox进行。



具体鉴权流程如下：

> The secure debug authentication supports either asymmetric or symmectric algorithm.
>
> 1.asymmetric: SHA256-SECP256R1, SM3-SM2
>
> 2.symmetric: AES-CMAC, SM4-CMAC

The secure debug flow is

1.Host sends debug request to eHSMthrough either UART or mailbox

2.eHSMsends challenge (UID + random number) toHost

3.Host sign the challenge with the debug key, and send back toeHSM

​	(a)asymmetric: sign with corresponding private key of ehsm debug key/soc debug key

​	(b)symmetric: calculate the CMAC value with ehsm debug key/soc debug key

4.eHSM verify the signiture with ehsm debug key/soc debug key

5.eHSM assert the o_ehsm_status corresponding bit to enable the eHSM/SoC debug port if verify pass.





<font color=red>#Q:鉴权分成eHSM鉴权和SoC鉴权两类？</font>

<font color=red>hsm有debug口？cm4有debug口？共用一个？</font>

<font color=red>文档中，host发送debug请求？？host是指？</font>





































---

# 术语解释：

root of trust :

> **根信任**是整个系统的信任基础。
>
> eHSM作为设备的根信任，负责确保关键的安全功能（如加密、密钥管理）与设备的其他部分硬件隔离开来，从而防止未经授权的访问或篡改。



 cryptographic services(TRNG,HASH, SKE and PKE) ：

> 高性能的加密引擎，用于执行复杂的加密算法（如TRNG、HASH、SKE和PKE）
