---
title: python基础语法
date: 2024-06-11 15:01:33
categories:
- ip开发环境搭建
- python脚本
tags:
- TBD
---



# 条件分支

```python
if condition1:
    # statement1
elif condition2:
    # statement2
else:
    #statement3
```

# 打印变量和文本

```python
    current_path = os.getcwd()
    print("\033[1;36m current_path=%s \033[0m"%current_path)
    output_file_path = os.path.join(current_path, 'data_info.txt')
    with open(output_file_path, "w") as data_info_f: //如果文件不存在会自动创建
        for section in sectionlist:
            # print("\033[1;36m section=%s \033[0m\n"%section)
            if section == ".riscv.attributes":
                continue
            if section == ".tohost":
                continue
            if section == ".fromhost":
                continue

            cmd1 = "readelf -x " + section + " " + elffile
            # print(cmd1)

            data_info = subprocess.check_output(cmd1, shell=True).decode()
            # print("\033[1;36m data_info=%s \033[0m"%data_info)
            # print(data_info, file=data_info_f)
            data_info_f.write(data_info + "\n")

            getData(data_info, section)
    data_info_f.close()
```

# 获取当前路径

current_path = os.getcwd()
